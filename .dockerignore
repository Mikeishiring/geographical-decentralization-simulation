# NOTE: .git is intentionally NOT excluded — needed for git-lfs pull during build.
# The Dockerfile removes .git after resolving LFS pointers.
.github
.venv
__pycache__
.pytest_cache
.mypy_cache
.ruff_cache

.benchmarks
.simulation_cache
tests/.artifacts
tmp_*

figures
latency_figures

explorer/node_modules
explorer/dist

# Test simulation data excluded from production image
dashboard/simulations/test
